


### **Question 3 – Overview (Focused on Slack Integration Review):**

The review is for the Slack integration within the role-based storefront application used for device provisioning. This component enables real-time approval workflows by sending interactive Slack bot notifications to the requestor’s manager when a device request is submitted.

The Slack app is configured with the following OAuth scopes to support its functionality:
- `app_mentions:read` – Detect when the app is mentioned to trigger workflows or respond with context.
- `channels:read` – Access metadata about channels to determine appropriate context or delivery options.
- `chat:write` – Post approval messages, alerts, and status updates in Slack channels or direct messages.
- `chat:write.public` – Post messages to public channels even if the app isn't a member.
- `commands` – Enable slash commands for testing or admin-triggered Slack interactions.
- `im:read` – Retrieve metadata about direct messages to the app.
- `im:write` – Send direct messages to users, typically used for reminders or confirmations.
- `team:read` – Access team-level metadata such as domain and workspace information for internal mapping.
- `users:read` – Retrieve user details for matching requestors and managers.
- `users:read.email` – Resolve Slack users by email address to map against enterprise directory and route notifications correctly.

This integration ensures timely manager decision-making, minimizes delays, and provides traceability. All manager responses (Approve/Reject) are securely logged and forwarded to the Remedy platform for ticket generation and downstream fulfillment. This Slack-based workflow is being reviewed for its functionality, data flow, scope usage, and compliance with enterprise security standards.



Got it — here’s the revised **Question 5 – Description** with **TLS** and **ARC group** details removed, and keeping the focus on Slack integration and WebSocket usage:

---

### **Question 5 – Description (Final Version with WebSocket):**

The Slack integration is a key component of our device provisioning storefront, designed to automate and simplify the manager approval workflow.

When a user submits a device request, the system sends an interactive Slack bot message to the user’s manager, which includes the request details along with **Approve** and **Reject** buttons. Once the manager responds, the action is logged, and if approved, a corresponding work order is automatically created in **Remedy**, GEICO’s internal IT service management platform.

The application also uses **WebSocket connections** to provide real-time updates to the user interface. As soon as the manager takes action in Slack, the requester sees the updated status reflected instantly in the storefront without needing to refresh the page.

The Slack app is configured with the following OAuth scopes to support these features:

- `app_mentions:read` – Detects mentions of the app to trigger contextual actions or responses.
- `channels:read` – Provides access to public channel metadata for routing and visibility.
- `chat:write` – Sends interactive messages, status updates, and confirmations.
- `chat:write.public` – Enables posting to public channels where the app isn’t a member.
- `commands` – Supports slash commands for testing or admin-initiated workflows.
- `im:read` – Allows reading metadata about direct messages with users.
- `im:write` – Enables sending direct messages to managers or users.
- `team:read` – Accesses basic workspace metadata for internal use.
- `users:read` – Retrieves user profiles to identify and match users and approvers.
- `users:read.email` – Maps Slack users to corporate identities via email address.

This integration creates a seamless, real-time approval workflow across Slack and Remedy, reducing manual steps and increasing efficiency for both requestors and managers.

---

Let me know if you'd like a shorter version for a summary field or a version that’s more technical.

---

