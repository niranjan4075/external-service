Update the Storefront asset automation pipeline to correct existing asset data issues and switch the data consumption pattern from “full load” to incremental. The pipeline should only consume newly created/updated records on each run, reducing duplicate processing and improving performance and accuracy for Storefront asset workflows.


The pipeline should consume only newly created or updated asset records instead of processing the full dataset each time.

Existing incorrect or stale asset data must be corrected without creating duplicates.

The updated workflow must be fully idempotent, ensuring reruns do not generate conflicting or repeated entries.

Incremental processing should reduce execution time and improve overall pipeline efficiency.

Validation checks must confirm accurate record counts and successful data correction after deployment.
